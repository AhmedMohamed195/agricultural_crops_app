import 'package:flutter/material.dart';

class SaleDateScreen extends StatelessWidget {
  const SaleDateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    
      body: const Center(child: Text("This is Sale Date Screen")),
    );
  }
}
